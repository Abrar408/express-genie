const Joi = require("joi");

const <%= entityName %>CreateSchema = Joi.object({
  query: Joi.object({}),
  params: Joi.object({}),
  body: Joi.object({name: Joi.string().required()}),
});

const <%= entityName %>UpdateSchema = Joi.object({
    query: Joi.object({}),
    params: Joi.object({bookId: Joi.number().required()}),
    body: Joi.object({name: Joi.string().optional()}).min(1),
  });

module.exports = {<%= entityName %>CreateSchema, <%= entityName %>UpdateSchema};